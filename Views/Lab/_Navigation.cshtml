@if (TempData["LabSuccess"] is string message)
{
    <div class="alert alert-success d-print-none" role="status"><strong>Saved successfully.</strong> @message</div>
}
@if (TempData["LabInfo"] is string info)
{
    <div class="alert alert-info d-print-none" role="status">@info</div>
}
