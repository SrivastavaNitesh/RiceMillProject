@{
    var layoutClaim = User.FindFirst("LayoutName")?.Value;
    if (!string.IsNullOrEmpty(layoutClaim) && layoutClaim != "_LayoutDefault")
    {
        Layout = layoutClaim;
    }
    else
    {
        Layout = "_Layout";
    }
}

