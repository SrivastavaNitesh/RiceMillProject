@model RiceMillProject.Models.Post
@{
    ViewData["Title"] = "Add Post";
}

<!-- Page Header -->
<div class="erp-page-header">
    <div class="erp-page-title-group">
        <div class="erp-page-icon">
            <i class="bi bi-briefcase-fill"></i>
        </div>
        <div>
            <h3 class="erp-page-title">ADD NEW DESIGNATION / POST</h3>
            <p class="erp-page-subtitle">Configure job designation title for organizational hierarchy</p>
        </div>
    </div>
    <a asp-action="Index" class="btn-erp-outline">
        <i class="bi bi-arrow-left me-1"></i> Back to List
    </a>
</div>

<div class="row justify-content-center">
    <div class="col-lg-6 col-md-8">
        <div class="erp-card">
            <div class="erp-card-header">
                <span><i class="bi bi-plus-circle text-primary me-2"></i> Designation Details</span>
            </div>
            <div class="erp-card-body">
                <form asp-action="Create" method="post" class="row g-3">
                    <div class="col-md-12">
                        <label asp-for="PostName" class="form-label">Post / Designation Name <span class="req-star">*</span></label>
                        <input asp-for="PostName" class="form-control" required placeholder="e.g. Senior Supervisor, Plant Engineer" />
                        <span asp-validation-for="PostName" class="text-danger"></span>
                    </div>
                    
                    <input type="hidden" asp-for="IsActive" value="true" />

                    <div class="col-12 d-flex justify-content-between border-top pt-3 mt-4">
                        <a asp-action="Index" class="btn-erp-secondary">
                            <i class="bi bi-x-circle me-1"></i> Cancel
                        </a>
                        <button type="submit" class="btn-erp-primary">
                            <i class="bi bi-check-circle me-1"></i> Save Post
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
